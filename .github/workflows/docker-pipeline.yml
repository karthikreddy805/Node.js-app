name: CI/CD Pipeline

on:

  push:
    branches: [ main ]

  jobs:
    build-test:
      runs-on: ubuntu-latest
      steps:
        - name: Checkout Code
          uses: actions/checkout@V3

        - name: Install Dependencies
          run: npm install
        - name: Run Tests
          run: npm test
          
